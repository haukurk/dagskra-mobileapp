/* @flow */

import { create } from 'apisauce';

/*
 * This is just an example where to put API libs.
 * I don't really use it.
 * apisauce is supported by reactotron. Awesomeee.
 */

const api = create({
  baseURL: 'https://example.com',
});

export default api;
